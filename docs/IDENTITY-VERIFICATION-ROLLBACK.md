# Rollback

If an external verification provider is unavailable, disable withdrawals at the existing financial configuration layer rather than restoring selfie authorization for withdrawals. Identity verification must fail closed; selfie/presence remains an activity anti-fraud mechanism only.
